@using VendorOrderTracker.Models;

<h1>Orders</h1>
@if (Model.Count == 0)
{
  <p>There are no orders for this vendor</p>
}
<ul>
  @foreach (Order order in Model)
  {
    <li><a href='/orders/@order.Id'>@order.Name</a></li>
  }
  
</ul>
<a href="/orders/new">Add a new order for this vendor</a>